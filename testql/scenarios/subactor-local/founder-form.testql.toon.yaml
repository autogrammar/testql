# SCENARIO: Founder response form via Control ingress (unauthenticated HTML)
# TYPE: gui
# VERSION: 1.0
# #description is an empty note paragraph (not visible). Token stays in the URL fragment.

GUI[6]{action, selector, value, wait_ms}:
  navigate, -, http://127.0.0.1:18081/founder/form, 400
  wait_for_count, #title, 1, 8000
  assert_visible, #title, -, -
  assert_visible, #close-window, -, -
  assert_text, body, Jednorazowy token, -
  stop, -, -, -
