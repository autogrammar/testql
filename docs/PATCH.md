# PATCH

## Purpose

PATCH command in the TestQL declarative testing DSL.

## Syntax

See `packages/dsl2testql/src/dsl2testql/schema/commands/patch.schema.json` for the canonical JSON Schema contract.

## Inputs

Defined by the command schema.

## Outputs

Defined by the command schema.

## Errors

See `docs/ERROR/` for error codes.

## Examples

See `examples/` directory.
